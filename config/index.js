const env     = require("./env");
const db      = require("./db");
const keys    = require("./keys");
const cookie  = require("./cookie");
const cors    = require("./cors");

module.exports = {
  env,
  db,
  keys,
  cookie,
  cors,
};
